{
  "name": "Python 3 (Streamlit App)",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bullseye",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "app.py"
      ]
    },
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "set -e; if [ -f requirements.txt ]; then python3 -m pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt; else pip install --no-cache-dir streamlit pandas numpy matplotlib xlsxwriter openpyxl xlrd; fi; echo '✅ Packages installed'",
  "postAttachCommand": {
    "server": "streamlit run app.py --server.address 0.0.0.0 --server.port 8501 --server.enableCORS false --server.enableXsrfProtection fa
